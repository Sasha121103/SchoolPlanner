<Window x:Class="SchoolPlanner.ScheduleArchiveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Архив расписаний" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
     
        ResizeMode="CanResize" Icon="/log.png">

    <Window.Resources>
        <!-- Стили -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" 
                Style="{StaticResource CardStyle}" 
                Background="White"
                Margin="0,0,0,10">
            <TextBlock Text="Архив расписаний" 
                      FontSize="20" 
                      FontWeight="Bold" 
                      Foreground="#FF9800"
                      Margin="15,10"/>
        </Border>

        <!-- Таблица с расписаниями -->
        <DataGrid x:Name="dgScheduleArchive" 
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1"
                  Background="White"
                  RowHeight="40"
                  FontSize="13"
                  SelectionMode="Single"
                  MouseDoubleClick="dgScheduleArchive_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Width="50" Binding="{Binding Number}"/>
                <DataGridTextColumn Header="Название" Width="200" Binding="{Binding ScheduleName}"/>
                <DataGridTextColumn Header="Период" Width="200" Binding="{Binding Period}"/>
                <DataGridTextColumn Header="Статус" Width="120" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Кол-во уроков" Width="100" Binding="{Binding LessonCount}"/>
                <DataGridTextColumn Header="Создано" Width="130" Binding="{Binding CreatedAt}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}" 
                Background="White"
                Margin="0,10,0,0">
            <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Всего записей: " Foreground="#666666"/>
                    <TextBlock x:Name="txtTotalSchedules" Text="0" Foreground="#FF9800" FontWeight="Bold" Margin="0,0,20,0"/>
                    <TextBlock Text="Двойной клик - просмотр деталей" Foreground="#999999" FontStyle="Italic"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        Content="Закрыть" 
                        Style="{StaticResource MainButtonStyle}"
                        Background="#666666"
                        Padding="30,8"
                        Width="120"
                        Click="btnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>