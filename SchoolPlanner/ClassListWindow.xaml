<Window x:Class="SchoolPlanner.ClassListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список классов" 
        Height="500" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"

        ResizeMode="CanResize" Icon="/log.png">

    <Window.Resources>
        <!-- Стили (можно скопировать из предыдущего окна) -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" 
                Style="{StaticResource CardStyle}" 
                Background="White"
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Список классов и количество учащихся" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          Foreground="#4CAF50"
                          Margin="15,10"/>
                <Button x:Name="btnExport" 
                        Grid.Column="1"
                        Content="📤 Экспорт в CSV" 
                        Style="{StaticResource MainButtonStyle}"
                        Background="#4CAF50"
                        Padding="15,8"
                        Margin="0,5,15,5"
                        Width="150"
                        Click="btnExport_Click"/>
            </Grid>
        </Border>

        <!-- Таблица с данными -->
        <DataGrid x:Name="dgClassList" 
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1"
                  Background="White"
                  RowHeight="35"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Width="60" Binding="{Binding Number}"/>
                <DataGridTextColumn Header="Название класса" Width="150" Binding="{Binding ClassName}"/>
                <DataGridTextColumn Header="Параллель" Width="80" Binding="{Binding Grade}"/>
                <DataGridTextColumn Header="Количество учеников" Width="150" Binding="{Binding StudentCount}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#2196F3"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Мальчиков" Width="100" Binding="{Binding BoysCount}"/>
                <DataGridTextColumn Header="Девочек" Width="100" Binding="{Binding GirlsCount}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя панель с итогами -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}" 
                Background="White"
                Margin="0,10,0,0">
            <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Всего классов: " Foreground="#666666"/>
                    <TextBlock x:Name="txtTotalClasses" Text="0" Foreground="#2196F3" FontWeight="Bold" Margin="0,0,20,0"/>
                    <TextBlock Text="Всего учеников: " Foreground="#666666"/>
                    <TextBlock x:Name="txtTotalStudents" Text="0" Foreground="#F44336" FontWeight="Bold"/>
                </StackPanel>
                <Button Grid.Column="1" 
                        Content="Закрыть" 
                        Style="{StaticResource MainButtonStyle}"
                        Background="#666666"
                        Padding="30,8"
                        Width="120"
                        Click="btnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>